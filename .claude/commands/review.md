Review the current state of the project. Check:
1. Does the code/output match the original intent?
2. Are there untested edge cases?
3. Are there hardcoded values that should be configurable?
4. Would this embarrass me if a client saw it?
Report findings as: PASS / WARN (with reason) / FAIL (with fix)